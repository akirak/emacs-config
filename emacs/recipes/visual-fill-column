;; Unfortunately, Nix currently doesn't support main as the default branch,
;; so you have to define a recipe for every such a repository.
;; In the future, this may be resolved in the upstream Nix, but I don't know.
(visual-fill-column :fetcher git :url "https://codeberg.org/joostkremers/visual-fill-column.git"
                    :branch "main")